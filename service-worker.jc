self.addEventListener("install", e => {
  e.waitUntil(
    caches.open("travel-log").then(cache =>
      cache.addAll([
        "./",
        "./index.html",
        "./style.css",
        "./script.js"
      ])
    )
  );
});
